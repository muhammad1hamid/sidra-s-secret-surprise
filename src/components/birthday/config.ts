/* ─────────────────────────────────────────────
   ▶ GLOBAL SETTINGS — edit these in one place
   ───────────────────────────────────────────── */

/** ▶ BIRTHDAY TARGET (month is 0-indexed: 7 = August) → August 5 at midnight */
export const getTarget = () => new Date(new Date().getFullYear(), 7, 5, 0, 0, 0);
